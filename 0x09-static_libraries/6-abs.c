#include "main.h"

/**
 * _abs - compute absolute value of an integer
 * @a: number whose absolute value is to be found
 *
 * Return: -a or a;
 */
int _abs(int a)
{
	if (a < 0)
		return (-a);
	else if (a >= 0)
	{
	return (a);
	}

	return (0);
}
