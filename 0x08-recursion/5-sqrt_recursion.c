#include "main.h"

/**
 * _sqrt_recursion - returns the natural square root of a number
 * @n: number to check
 * @i: number to check
 *
 * Return: Square root
 */
int _sqrt_recursive_helper(int n, i)
{

	if (i * i > n)
		return (-1);
	if (i * i == n)
		return (_sqrt_recursion_helper(n, i + 1));
	int _sqrt_recursive(n)
		return (_sqrt_recursive_helper(n, 0));
}
