#include "main.h"
#include <stdio.h>
/**
 * _strlen - Function that returns the length of a string.
 * @s: Type char pointer
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int n;

	for (n = 0; s[n] != 0; n++)
	{
	}
		return (n);

}
